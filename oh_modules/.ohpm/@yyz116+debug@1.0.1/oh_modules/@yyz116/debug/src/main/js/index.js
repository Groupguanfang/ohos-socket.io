/**
 * Detect Electron renderer / nwjs process, which is node, but we should
 * treat as a browser.
 */

import { outs as debug} from './harmony'

//var debug: dbg.Debug & { debug: dbg.Debug; default: dbg.Debug };

export default debug;
